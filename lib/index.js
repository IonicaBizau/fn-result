"use strict";

/**
 * fnResult
 * Take an (a)sync function result and pass it forward.
 *
 * @name fnResult
 * @function
 * @param {Number} a Param descrpition.
 * @param {Number} b Param descrpition.
 * @return {Number} Return description.
 */
module.exports = function fnResult (a, b) {
    return a + b;
};